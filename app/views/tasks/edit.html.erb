<h1>id: <%= @task.id %> タスクの編集</h1>

<%= form_for(@task) do |f| %>
  <%= f.label :content, 'タスク' %>
  <%= f.text_field :content %>
  
  <%= f.submit '登録' %>
<% end %>
<br>
<br>
<%= link_to '一覧に戻る', tasks_path %>
